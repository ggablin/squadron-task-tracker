{
  "name": "squadron-tracker-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for the 108 CES UTA Task Tracker — drives the tracker's HTTP API as a signed-in member",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/client.test.js test/tools.test.js",
    "smoke": "node test/smoke.js",
    "smoke:prep": "node test/prep-smoke.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  }
}
